# Must match gentorial.server.config.ts -> apiKeyEnv.
OPENAI_API_KEY=

# Optional public URL when the Node service is deployed separately.
# VITE_GENTORIAL_ENDPOINT=https://api.example.com/api/gentorial/generate

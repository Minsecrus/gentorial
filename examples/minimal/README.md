# Gentorial minimal example

纵向贯通示例：静态 HTML 中始终存在 `switch` 概念锚点；导航栏提供全局 Preferences / BYOK 弹窗，标题旁的液态小球按真实运行状态触发生成。未配置 BYOK 时生成区域明确报错；填写密钥、模型和 Base URL 后调用真实提供方，并在主讲解与追问中流式解析标准 Markdown。

```bash
pnpm build
pnpm dev
```

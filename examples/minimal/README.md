# Gentorial minimal example

纵向贯通示例：静态 HTML 中始终存在 `switch` 概念锚点；导航栏提供全局 Preferences / BYOK 弹窗，标题旁的液态小球按真实运行状态触发生成。无密钥时使用确定性 mock，填写会话密钥、模型和 Base URL 后改用真实提供方，并在主讲解与追问中流式显示文本。

```bash
pnpm build
pnpm dev
```

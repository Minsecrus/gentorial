{
  "name": "@gentorial/server",
  "version": "0.2.0",
  "description": "Server-managed generation, credentials, and shared result caching for Gentorial.",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --sourcemap --clean && tsc --emitDeclarationOnly",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root ../.. --config vitest.config.ts packages/server/src",
    "pack:check": "pnpm pack --dry-run"
  },
  "keywords": [
    "gentorial",
    "server",
    "cache",
    "education"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@gentorial/ai": "workspace:^",
    "@gentorial/core": "workspace:^"
  }
}
